# Devenv
.devenv*
devenv.local.nix

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# JavaScript runtimes
node_modules/
package-lock.json
pnpm-lock.yaml

# raw notes I jotted down to help brainstorm what to put in
rawnotes/
